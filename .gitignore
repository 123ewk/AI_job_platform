.boss_profile/
__pycache__/
*.pyc
.env
**/.env
.DS_Store
reports/
.claude/
.boss_profile/
.pytest_cache/
.ruff_cache/
boss_data.db
*.bak
*.db-shm
*.db-wal

# Runtime artifacts
*.log
boss_page.html
search_result.json
NUL

# Secrets — never commit
*.pem
*.key
id_rsa*
id_ed25519*
id_dsa*
id_ecdsa*
*.p12
*.pfx
secrets/
.credentials/

# Local-only backup of the original radar git history (never push)
.git.radar-backup/

# Private interview prep bank (local-only, never push to GitHub)
docs/INTERVIEW_QUESTIONS.md

# probe_resume_dialog.py 取证产物（本地）
probe_resume_dialog_result.json
